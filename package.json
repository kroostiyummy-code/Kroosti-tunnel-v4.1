{
  "name": "kroosti-tunnel-v4.1",
  "version": "1.0.0",
  "description": "Site toitrenov — entreprise de rénovation toiture, façade, isolation en Nouvelle-Aquitaine",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./assets/site.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./assets/site.css --watch",
    "dev": "tailwindcss -i ./src/input.css -o ./assets/site.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
